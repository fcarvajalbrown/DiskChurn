[project]
name = diskchurn
author = Felipe Carvajal Brown

[structure]
;
; diskchurn/
; ├── Cargo.toml          [done] package manifest, dependencies
; ├── Cargo.lock          [done] tracked (binary crate)
; ├── build.rs            [done] sets /SUBSYSTEM:WINDOWS, no console window
; ├── structure.ini       [done] this file
; ├── README.md           [done] project overview, badges, build instructions
; ├── CLAUDE.md           [done] Claude context, coding rules, architecture notes
; ├── AGENTS.md           [done] agent instructions, file map, key types
; ├── docs/
; │   └── prd.md          [done] full product requirements document
; ├── src/
; │   ├── lib.rs          [done] crate root, all modules declared as pub mod
; │   ├── main.rs         [done] eframe::run_native bootstrap
; │   ├── types.rs        [done] FileNode, FolderStats, ChurnClass, EntropyClass, DiskSnapshot
; │   ├── scanner.rs      [done] MFT walk (DeviceIoControl) + walkdir fallback, rayon parallel
; │   ├── classifier.rs   [done] ChurnClass assignment + linear regression for Hot growth projection
; │   ├── entropy.rs      [done] 64 KB sampler, Shannon entropy calc, EntropyClass assignment
; │   ├── treemap.rs      [done] Squarify layout, egui painter rendering, delta paint variant
; │   ├── delta.rs        [done] FolderDelta, compute(), apply_min_filter()
; │   ├── history.rs      [done] bincode snapshot persistence, list/load/save/prune
; │   └── app.rs          [done] egui App impl, panels, drive dropdown, compare toolbar, filters
; └── tests/
;     ├── classifier.rs   [done] aggregation, Cold/Hot/Volatile classification tests
;     ├── delta.rs        [done] compute axioms, apply_min_filter tests
;     ├── entropy.rs      [done] Shannon math, boundary threshold tests
;     ├── treemap.rs      [done] layout edge cases, area conservation, fmt_delta tests
;     └── history.rs      [done] bincode round-trip, field preservation tests
