[project]
name = diskchurn
author = Felipe Carvajal Brown

[structure]
;
; diskchurn/
; ├── Cargo.toml          [done] package manifest, dependencies
; ├── build.rs            [done] sets /SUBSYSTEM:WINDOWS, no console window
; ├── structure.ini       [done] this file
; └── src/
;     ├── types.rs        [done] FileNode, FolderStats, ChurnClass, EntropyClass, DiskSnapshot
;     ├── scanner.rs      [ ]    MFT walk (DeviceIoControl) + walkdir fallback, sends FileNode batches
;     ├── classifier.rs   [ ]    ChurnClass assignment + linear regression for Hot growth projection
;     ├── entropy.rs      [ ]    64 KB sampler, Shannon entropy calc, EntropyClass assignment
;     ├── treemap.rs      [ ]    Squarify layout, renders via egui painter
;     ├── app.rs          [ ]    egui App impl, layout, panels, drive dropdown, state machine
;     └── main.rs         [ ]    eframe::run_native bootstrap, window config
